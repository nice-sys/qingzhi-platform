# 青知共享平台 · 概要设计文档

> 文档版本：v1.0
> 整理日期：2026 年 8 月
> 文档类型：**概要设计（Architectural / High-level Design）**
> 关联文档：
> - [需求分析 - 业务需求.md](./需求分析%20-%20业务需求.md)
> - [需求分析 - 用户需求.md](./需求分析%20-%20用户需求.md)
> - [需求分析 - 系统需求.md](./需求分析%20-%20系统需求.md)
> - [详细设计.md](./详细设计.md)
> - [PRD_青知共享平台.md](./PRD_青知共享平台.md)
> 代码位置（已实现）：
> - 后端：[backend/](../backend)
> - 前端：[frontend/](../frontend)

---

## 1. 设计目标与原则

### 1.1 设计目标

| # | 目标 | 说明 |
|---|---|---|
| G-1 | **结构清晰、分层解耦** | 严格按 Controller → Service → Mapper 三层，跨层访问只能"自上而下"，绝不反向依赖。 |
| G-2 | **安全合规基线** | JWT + 白名单 + 后端 Service 层兜底权限断言 + 统一异常/错误码 + MD5 加密 + 防目录穿越。 |
| G-3 | **中文零乱码** | utf8mb4 三层（SQL BOM + Hikari init-SQL `SET NAMES utf8mb4` + JDBC URL `characterEncoding=UTF-8`）100% 保障中文存储与检索。 |
| G-4 | **可扩展（Open/Closed）** | 文件存储 MVP 本地；未来 S3 只需新增 `S3FileServiceImpl`；新增角色只需扩展 `RoleEnum` + `PermissionUtil`。 |
| G-5 | **快速交付 + 可维护** | MyBatis 纯 XML 手写 SQL 透明可控；错误码/响应体全系统统一；前端 Component 化（`<ResourceCard>` / `<FileUpload>` / `<Pagination>` 可复用）。 |

### 1.2 设计原则（SOLID + 架构常用）

- **单一职责 SRP**：6 个 Controller / 6 对 Service 接口一一对应 6 大业务模块，不交叉。
- **依赖倒置 DIP**：Controller 依赖 Service 接口；Service 依赖 Mapper 接口，不依赖具体实现类。
- **开闭 OCP**：`@RateLimit` AOP 切面可通过注解任意开关；统一响应 `Result<T>` 不侵入业务代码。
- **里氏替换 LSP**：ServiceImpl 实现 Service 接口，任何用 Service 接口的地方都能替换为 Impl。
- **接口隔离 ISP**：`UserService` 不含管理台方法，管理台专属方法集中在 `AdminService`，学生不会拿到"重置密码"接口。
- **约定优于配置 CoC**：Controller 路径统一去 `/api` 前缀，context-path 在 `application.yml` 一处管理，杜绝双写路径。
- **零魔法数字**：错误码 → `ResponseCodeEnum`；角色 → `RoleEnum`；审核状态 → `ReviewStatusEnum`。

---

## 2. 系统总体架构（C4 容器视图）

### 2.1 架构分层图（三层 + 横切关注点）

```
┌─────────────────────────────────────────────────────────────────────┐
│  [BROWSER]  Chrome / Edge / Safari / 移动端 Web                     │
│         Vue 3 SPA (Vite + Pinia + Vue Router 4 + Element Plus 2.8) │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ Views (26 个)  │  Layout + Sidebar  │  Components (7 复用)    │   │
│  └──────────────────────┬───────────────────────────────────────┘   │
│                         │  request.js (Axios baseURL=/api)          │
└─────────────────────────┼───────────────────────────────────────────┘
                          │ HTTP :5173(Vite) / :80(Nginx) → :8080
                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│  [APPLICATION]  Spring Boot 3.4.1 (Tomcat 内嵌, context-path=/api)   │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  Controllers (6)  Auth / User / Resource / File / Favorite   │   │
│  │                └── Admin  (管理台专属)                        │   │
│  ├──────────────────────────────────────────────────────────────┤   │
│  │  Services / Impl (12)   AuthService Impl, ResourceService..  │   │
│  │  横切  AOP @RateLimit  │  JwtInterceptor  │  GlobalException │   │
│  ├──────────────────────────────────────────────────────────────┤   │
│  │  MyBatis XML  Mappers (4)  User / Resource / Favorite / FS   │   │
│  └──────────────────────────────────────────────────────────────┘   │
│            │                              │                         │
│            ▼                              ▼                         │
│  ┌─────────────────┐          ┌────────────────────────┐            │
│  │ MySQL 8.0+ :3306│          │ 本地文件系统存储        │            │
│  │ qingzhi_platform│          │ backend/uploads/       │            │
│  └─────────────────┘          └────────────────────────┘            │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈选型矩阵（对照 PRD §3.3）

| 层 | 技术 | 版本 | 选型理由 |
|---|---|---|---|
| 前端框架 | **Vue** | 3.5 | Composition API，学习曲线低，Element Plus 生态完善。 |
| 前端构建 | **Vite** | 5.4 | HMR 极速，开箱即用 ES Module 构建。 |
| UI 组件库 | **Element Plus** | 2.8 | 与 Vue 3 官方协作，表格 / 表单 / Dialog 组件齐全。 |
| 状态 | **Pinia** | 2 | Vue 官方推荐 Store，比 Vuex 更简洁（getters/actions 一体化）。 |
| 路由 | **Vue Router** | 4 | `meta.roles` 守卫按角色过滤。 |
| HTTP | **Axios** | 1.7 | 请求拦截注入 Token、响应拦截统一解包 `Result<T>` / 处理 1001。 |
| 后端框架 | **Spring Boot** | **3.4.1 GA** | PRD 指定；Actuator / Validation / AOP 开箱即用。 |
| ORM | **MyBatis Spring Boot Starter** | 3.0.4（纯 XML） | PRD 指定；手写 SQL，无 JPA 黑盒。 |
| 数据库 | **MySQL** | 8.0+ / 8.4 | PRD 指定；`utf8mb4_0900_ai_ci` 中文排序规则完善。 |
| 鉴权 | **JJWT 0.12.6** | 0.12.6 | 无状态 JWT，HMAC-SHA256。 |
| Excel | **Apache POI OOXML** | 5.2.5 | `.xlsx` 最成熟解析库。 |
| 工具 | **Lombok** | 1.18.44 | 兼容 JDK 26，无 Getter/Setter 样板。 |

---

## 3. 模块划分与职责（Bounded Context）

### 3.1 后端模块划分

| 模块 ID | 模块名 | 包根 / 组件根 | 职责与对外接口（`/api/**`） | 对应 Service / Controller |
|---|---|---|---|---|
| M-A | **认证与登录** | `controller.AuthController` + `service.AuthService` | `POST /auth/register`、`POST /auth/login`、（预留登出） | `AuthServiceImpl` + `AuthController` |
| M-U | **用户与个人中心** | `controller.UserController` + `service.UserService` | `GET/PATCH /user/info`、`POST /user/avatar`、`POST /user/change-password` | `UserServiceImpl` + `UserController` |
| M-R | **资源生命周期** | `controller.ResourceController` + `service.ResourceService` | 列表 / 详情 / 发布 / 修改 / 删除 / 课程字典 | `ResourceServiceImpl` + `ResourceController` |
| M-FI | **文件能力** | `controller.FileController` + `service.FileService` | `POST /file/upload`（限流/秒传）、`GET /file/download/{resourceId}`（鉴权+原子计数） | `FileServiceImpl` + `FileController` |
| M-F | **收藏体系** | `controller.FavoriteController` + `service.FavoriteService` | `POST /favorite/add`、`POST /favorite/remove`、`GET /favorite/list` | `FavoriteServiceImpl` + `FavoriteController` |
| M-AD | **管理后台** | `controller.AdminController` + `service.AdminService` | 审核通过/拒绝、用户CRUD、重置密码、Excel导入、资源全量管理 | `AdminServiceImpl` + `AdminController` |
| M-SEC | **安全横切**（AOP / Interceptor） | `config / interceptor / aspect / exception` | JWT 签发+校验、公开 GET 匿名、`@RateLimit` 限流、全局异常处理、角色断言 | `JwtInterceptor` + `RateLimitAspect` + `GlobalExceptionHandler` + `PermissionUtil` |
| M-COM | **通用基础** | `common / dto / entity / enums / utils / vo` | `Result<T>` / `PageResult<T>` / `Constants` / `ResponseCodeEnum` / `PasswordUtil` / 全部 30 个 DTO | 全系统共享 |

### 3.2 前端模块划分（按 PRD 3.3.3 目录）

| 模块 ID | 模块名 | views 目录 / 组件根 | 路由范围 | 组件依赖 |
|---|---|---|---|---|
| F-A | **认证（登录/注册）** | `views/auth/*` | `/login` `/register`（白名单） | `validate.js` + `auth.js` API |
| F-CORE | **布局框架** | `layout/*` + `App.vue` | `Layout` 包裹 `/dashboard` `/resource` `/profile` `/admin` | `<Header>` `<Sidebar>` + `userStore.getters.isAdmin` |
| F-DASH | **首页 Dashboard** | `views/dashboard/Index.vue` | `/dashboard` | `resource.js` `user.js` 统计接口（MVP：内置假数据卡片 238/201/1236/1024） |
| F-R | **资源中心** | `views/resource/*` + `components/resource/*` + `components/common/FileUpload.vue` | `/resource/list` `:id` `publish` `update/:id` | `<ResourceCard>` `<ResourceFilter>` `<FileUpload>` + `resource.js` |
| F-P | **个人中心** | `views/profile/*` | `/profile/info` `password` `resources` `favorites` | `<StatusTag>` `<Pagination>` + `favorite.js` |
| F-AD | **管理后台（仅 Admin）** | `views/admin/*` + `components/admin/ImportResult.vue` | `/admin/users` `review` `resources` `import` | `<ImportResult>`（导入行级错误明细）+ `admin.js` |
| F-STATE | **全局状态与请求** | `stores/*` + `api/*` + `utils/*` | — | `userStore` (token+持久化) / `request.js`（Axios Token 注入+Result解包） |

---

## 4. 核心业务流程图（Sequence 摘要）

### 4.1 发布 → 审核 → 检索 → 下载闭环（主链路）

```
[学生 S]  ──POST /file/upload (Multipart)──▶  [FileController]
 FileContr  ──MD5→hitQuickUpload?──────────▶  [FileServiceImpl]──▶ file_storage (uk_hash)
 FileContr  ◀──{fileStorageId, hitQuickUpload}── 学生
[学生 S]  ──POST /resource/publish {title, course, 6文件字段 or fileStorageId}──▶ [ResourceController]
 ResourceC  ──回填 6 字段（若仅 fileStorageId）──▶ [ResourceServiceImpl]
 ResourceC  ──INSERT review_status=0 PENDING ──▶ MySQL resource
 ResourceC  ◀──{resourceId}── 学生

[管理员 A] ──POST /admin/resources/review {resourceId, approve:true}──▶ [AdminController]
 AdminC  ──assertAdmin(role=0)──▶ PermissionUtil
 AdminC  ──UPDATE review_status=1 reviewer_id=uid review_time=NOW()──▶ MySQL
 AdminC  ◀──code=1 reviewed=true

[学生/匿名] ──GET /resource/list?keyword=真题&course=软件工程────────▶ [ResourceController]
 ResourceC  ──动态 <if> SQL: review_status=1 + keyword + course + startTime~endTime ▶ MySQL
 ResourceC  ◀──PageResult<ResourceListResponse> [10条, total=xx]

[学生 S]  ──GET /file/download/5 (resourceId=5)──▶ [FileController]
 FileContr  ──downloadResource(resourceId, uid, role) ──▶ [ResourceServiceImpl]
   (1)鉴权：review_status=1 OR 本人 OR Admin ✔
   (2)原子自增：UPDATE resource SET download_count = COALESCE(dc,0)+1 WHERE id=5; （行锁级）
   (3)resolveFile(filePath)：normalize + startsWith(uploadsRoot) ✔ 防目录穿越
 FileContr  ◀──200 application/xxx Content-Disposition filename=xxx.pdf（浏览器自动保存）
```

### 4.2 登录 → JWT → 路由守卫 → 鉴权 全链路

```
[浏览器]  ──POST /api/auth/login (username=Admin, pwd=Admin2026)──▶  [AuthController]
 AuthC  ──SELECT user WHERE username=Admin ──▶ MySQL
 AuthC  ──MD5 匹配 + 未锁定 ──▶ JwtUtil.generateToken(uid=1, role=0, 24h)
 AuthC  ◀──{ token: eyJhbG..., userInfo: {id:1,role:0,username:"Admin",name:"超级管理员",...}}
[request.js] 拦截响应 code=1 → userStore.setLoginData(token, userInfo) → localStorage 写入

[浏览器]  后续请求 GET /api/admin/users
[request.js] 请求拦截 → headers.Authorization = 'Bearer '+ user.token
[JwtInterceptor.preHandle]：
  ① 不在白名单（/admin/users 不在）
  ② 正则 isPublicGet?（不是 resource/list 等）→ 必须有效 JWT
  ③ JwtUtil.resolve → uid=1 role=0 → request.setAttribute(CURRENT_USER_ID, 1L, ROLE, 0)
  ④ return true 放行

[AdminController.listUsers()]：
  JwtInterceptor.getCurrentUserId() = 1
  PermissionUtil.checkUserManagePermission(role=0) ✔
  userMapper.selectPage(...) → PageResult → Result.success(PageResult) → 返回前端

[Vue Router] beforeEach：
  to.meta.roles = [ROLE.ADMIN]
  userStore.role = 0 (ADMIN) → 匹配 ✔ → next() 渲染 UserManage.vue
  (若 role=2 学生 → else → next('/dashboard') + Toast '无权限')
```

---

## 5. 数据库概要设计（ER 图 + 约束摘要）

### 5.1 ER 关系图（4 张核心表）

```
 ┌────────────────────────────┐
 │            user            │
 ├────────────────────────────┤
 │ id              PK BIGINT  │◀──────────┐
 │ username        UNIQUE     │           │ 上传者：1-N
 │ password        CHAR(32)   │           │
 │ role            TINYINT    │           │
 │ status/lock_time           │           │
 │ phone / email              │           │
 │ department/major           │           │
 │ create_time                │           │
 └────────────────────────────┘           │
          ▲                               ▼
          │ 审核人 1-N       ┌─────────────────────────────────────┐
          │                 │             resource                │
          │                 ├─────────────────────────────────────┤
          └── review_admin_id│ id         PK BIGINT               │
                            │ uploader_id FK(user.id) INDEX       │
                            │ title / description / course        │
                            │ 6 文件字段 (name/path/size/ext/hash) │
                            │ review_status   TINYINT INDEX       │
                            │ reject_reason / reviewer / time     │
                            │ download_count INT +1原子            │
                            │ create_time INDEX DESC              │
                            └─────────────────────────────────────┘
                                        ▲            ▲
                                        │            │ 收藏关系 N-M
                                        │            ▼
                              ┌─────────────────────────────┐
                              │          favorite           │
                              ├─────────────────────────────┤
                              │ user_id     FK PK_UK1       │
                              │ resource_id FK PK_UK2       │
                              │ create_time                 │
                              └─────────────────────────────┘

               ┌────────────────────────────────────┐
               │            file_storage            │
               ├────────────────────────────────────┤
               │ id               PK BIGINT          │
               │ file_hash        UNIQUE CHAR(32)   │◀──── MD5 秒传去重
               │ file_path / file_size / file_ext   │
               │ original_file_name (UTF-8 原始名)  │
               │ reference_count INT                │
               │ create_time                        │
               └────────────────────────────────────┘
```

### 5.2 核心索引与约束汇总

| 约束/索引 | 在表 | 作用 | 保障的设计目标 |
|---|---|---|---|
| `uk_username` UNIQUE | user | 同号注册失败 → 2001 | G-2 账号唯一性 |
| `uk_file_hash` UNIQUE | file_storage | 秒传去重 | G-3/5 节省磁盘 |
| `unique_uk_user_resource` UNIQUE | favorite | 重复收藏幂等 | G-2 数据一致性 |
| `idx_resource_uploader` | resource | 我的资源分页速度 | G-4 性能 |
| `idx_resource_course / idx_create_time` | resource | 关键词+课程+时间筛选 | NFR-P02 200ms |
| `idx_review_status` | resource | 管理员审核按状态过滤 | 体验 |

---

## 6. 接口设计概要（High-level API）

### 6.1 契约总览

- **全局 base URL**：`/api`（Spring Boot `server.servlet.context-path` 统一前缀，Controller 本身不写 `/api` 防双写）
- **统一响应体**：`{ code:number, message:string, data:T }`
  - `code=1` 成功，`Result.success(data)`
  - 其他失败：全局枚举 `ResponseCodeEnum`（1001 未登录 / 1002 无权限 / 2001 账号存在 / 5003 限流 / …）
- **分页统一封装**：`PageResult<T> { list:T[], total:long, pageNum:int, pageSize:int }`
- **鉴权方式**：`Authorization: Bearer <JWT>`，除白名单 + 公开 GET 匿名外必传
- **CORS**：后端 WebConfig 放行所有方法 + Header + 预检 3600s

### 6.2 模块 API 分组

```
 /auth/register, login                  ── M-A 认证
 /user/info GET|PATCH, /user/avatar, /user/change-password  ── M-U 个人
 /resource/list|{id}, publish, update, delete, /courses, /stats    ── M-R 资源
 /file/upload, /file/download/{resourceId}                     ── M-FI 文件
 /favorite/add, remove, list                               ── M-F 收藏
 /admin/resources/review, /admin/users CRUD|reset|import, /admin/resources ── M-AD 管理台
 /actuator/health                                           ── M-COM 监控
```

---

## 7. 横切关注点设计（Cross-cutting）

| 关注点 | 实现方案 | 位置 |
|---|---|---|
| **安全-JWT** | JJWT HS256；JwtInterceptor 写入 request attribute；Controller 用静态方法取 | `JwtConfig` + `JwtInterceptor` |
| **安全-权限** | Service 层统一 `PermissionUtil.assertAdmin / assertAdminOrSelf` | `PermissionUtil` + 各 ServiceImpl |
| **安全-限流** | `@RateLimit(limit=6, windowSeconds=60)` AOP → `ConcurrentHashMap<key, 请求时间列表>` 滑动窗口 | `@RateLimit` 注解 + `RateLimitAspect` |
| **异常统一** | `@RestControllerAdvice` + 捕获 Throwable → `Result.fail(枚举/消息)`，永不抛 Tomcat 500 | `GlobalExceptionHandler` |
| **参数校验** | DTO `@Valid` + 自定义 `RegisterRequest.isInvalidPassword()`；全局 MethodArgumentNotValid → `PARAM_ERROR code=2` | `dto.request.*` + `BusinessException` 静态断言 |
| **日志** | MyBatis 开发期 `StdOutImpl`；生产默认 INFO；Actuator /health 暴露 | `application.yml` profile 配置 |
| **字符集** | SQL BOM + init.sql 开头 8 条 `SET NAMES utf8mb4`；Hikari `connection-init-sql=SET NAMES utf8mb4 COLLATE utf8mb4_0900_ai_ci`；JDBC `characterEncoding=UTF-8` | `init.sql` / `application.yml` |
| **中文乱码兜底** | PowerShell 执行 init.sql 必须 `cmd /c mysql.exe --default-character-set=utf8mb4` | README / init.sql 注释 |
| **可观察性** | `/actuator/health` UP/DOWN 健康检查 | spring-boot-starter-actuator |

---

## 8. 部署与运行架构

### 8.1 开发环境（当前已验证）

```
 Student Browser :5173  ──Vite Dev Proxy "/api/*"──▶ Spring Boot :8080 ──JDBC UTF-8──▶ MySQL :3306
 Admin Browser   :5173               │                                           │
                                     ▼                                           ▼
                         frontend/src/views + Pinia          backend/uploads/YYYY/MM/DD/<hash>.<ext>
```

- 启动顺序：MySQL → `backend mvn spring-boot:run` → `frontend npm run dev` → 冒烟 `smoke.ps1`
- 验证基线：`/api/actuator/health = UP` + `/login` Admin/Admin2026 跳转 Dashboard

### 8.2 生产环境建议（概要级）

```
  用户 HTTPS ─▶ Nginx :80/443 (SSL offload + gzip + History try_files /index.html)
           ├─▶ 静态资源   /var/www/qingzhi/dist/*   (frontend npm run build)
           └─▶ 反向代理 /api/* ──▶ 内网 Spring Boot 实例集群 (1~N 台)
                                          │
                                          ├──▶ 内网 MySQL 主从 (utf8mb4)
                                          └──▶ 对象存储 (S3/OSS) 替换 FileServiceImpl 本地
```

### 8.3 配置外置（生产必做）

| 配置项 | 占位符写法 | 环境变量名 |
|---|---|---|
| DB 密码 | `${QINGZHI_DB_PASSWORD:默认}` | QINGZHI_DB_PASSWORD |
| JWT 密钥 | `${QINGZHI_JWT_SECRET:默认256bit}` | QINGZHI_JWT_SECRET |
| 上传根目录 | `${QINGZHI_UPLOADS_ROOT:./uploads}` | QINGZHI_UPLOADS_ROOT |

---

## 9. 安全与合规风险矩阵（概要级）

| 风险 | 影响 | 本设计应对 | 所在模块 |
|---|---|---|---|
| JWT 伪造 / 泄露 | 越权 | 密钥生产环境变量覆盖；过期时间 24h；后端每次都解析 | M-SEC |
| SQL 注入 | 拖库 | MyBatis `#{}` 全链路；模糊用 `CONCAT('%', #{kw}, '%')`；禁止 `${}` | M-COM/Mapper |
| 目录穿越（下载 `../../etc`） | 读系统敏感文件 | `resolveFile` normalize + `startsWith(uploadsRoot)` 检查 | M-FI |
| 上传大文件撑爆磁盘 | DoS | 50MB 上限（5001）+ MD5 秒传 + 6 次/分限流（5003） | M-FI |
| 枚举用户名 | 社工 | 登录失败统一 2002（不区分"账号不存在/密码错误"） | M-A |
| 越权操作他人资源 | 数据泄露 | Service 层 assertAdminOrSelf；不依赖前端隐藏按钮 | M-SEC + M-R |
| 中文乱码 1366 | 体验 | utf8mb4 三层对齐 + README 给出 Windows 正确执行命令 | init.sql / yml |

---

## 10. 非功能需求概要落地

| NFR | 概要落地路径 | 验证方法 |
|---|---|---|
| NFR-P01~P05 性能 | 5 核心索引 + MyBatis 动态 `<if>` + 原子 SQL 单语句自增 + 限流 | JMeter / Smoke-④ HTTP 200 |
| NFR-S01~S07 安全 | §9 表格 | 负向用例集 8 条 Postman |
| NFR-A01~A04 可用性 | Element Plus 响应式 + utf8mb4 保障 + History 模式 fallback | Chrome/Edge 真机 + 中文 CRUD |
| NFR-M 可维护 | 枚举零魔法数 + 6 对接口 + 统一 Result/PageResult + Component 7 个 | IDE 全项目 Grep |
| NFR-D 可部署 | JDK release=17 跨平台；Path API 不写死分隔符 | Windows/macOS/Linux 构建与 smoke |

---

## 11. 可追溯矩阵（概要级）

| 业务需求 BR | → | 用户用例 UC | → | 模块划分（本文 §3） |
|---|---|---|---|---|
| BR-A01 双通道注册 / BR-A03 预置 | → | UC-001 / 002 | → | M-A 认证 + M-AD Excel 导入 + M-U 个人 |
| BR-R01/R02 发布+审核流转 | → | UC-003 / 004 | → | M-R 资源 + M-AD 审核 + M-FI 上传 |
| BR-R03 检索 / BR-R04 原子下载 | → | UC-005 | → | M-R 列表/详情 + M-FI 下载 |
| BR-F01 / BR-F02 收藏 | → | UC-006 | → | M-F 收藏 |
| BR-AD04 批量导入 | → | UC-007 | → | M-AD 管理台 + `ExcelUtil` |
| BC-05 公开 GET 匿名 | → | 匿名游客 | → | M-SEC JwtInterceptor.isPublicGet + 白名单 |
