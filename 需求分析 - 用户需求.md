# 青知共享平台 · 需求分析文档——用户需求

> 文档版本：v1.0
> 整理日期：2026 年 8 月
> 文档类型：**需求分析 — 用户需求（User Requirement / Use Case View）**
> 关联文档：[需求分析 - 业务需求.md](./需求分析%20-%20业务需求.md)、[PRD_青知共享平台.md](../PRD_青知共享平台.md)

---

## 1. 文档定位与阅读指引

| 维度 | 说明 |
|---|---|
| **文档目的** | 从**最终使用用户 / 管理员视角**，描述**谁（角色）在什么场景下，通过什么步骤，想要完成什么任务，期望得到什么结果**。是业务需求（BR）向系统功能需求（FR）过渡的桥梁。 |
| **读者对象** | 产品经理、UX/UI 设计师、前端工程师、测试工程师、用户培训讲师。 |
| **核心方法** | 本节采用「角色画像（Persona）→ 角色用例矩阵 → 关键用例展开（UC-xxx）」的标准用户需求表达。 |
| **验证基线** | 每条用例的「基本流」必须可被**手动操作 / smoke 脚本 / 浏览器自动化**复现通过。 |

---

## 2. 角色画像（Personas）

### 2.1 平台管理员（林老师）——角色：Admin（0）

| 项 | 描述 |
|---|---|
| 画像 | 男，45 岁，信息中心技术支持老师，**负责平台账号与合规**。 |
| 目标 | ① 快速批量导入新学期师生账号；② 及时审核资源避免错漏资料公开；③ 处理学生/教师反馈（锁定账号、重置密码）。 |
| 痛点 | 每学期开学要手动建 500+ 个账号，易错；审核资源要打开 3 个系统来回切。 |
| 核心诉求 | **一键 Excel 导入 + 行级错误回显**；**审核通过/拒绝按钮在同一行可直接操作**。 |

### 2.2 课程教师（王老师）——角色：Teacher（1）

| 项 | 描述 |
|---|---|
| 画像 | 女，32 岁，计算机学院讲师，主讲「软件工程」「数据库系统」。 |
| 目标 | ① 把 PPT/历年真题发布到对应课程下；② 看看学生下载了多少次；③ 收集优质笔记给下一届复用。 |
| 痛点 | 过去把 PPT 放 QQ 群，过一学期就过期；版本更新要重复发。 |
| 核心诉求 | **发布资源能关联课程**；**下载数统计可见**；**修改自己的课件不用重新审核（MVP：需重新审核，见 BR-R02 状态回退规则）**。 |

### 2.3 在读学生（小李）——角色：Student（2）

| 项 | 描述 |
|---|---|
| 画像 | 男，20 岁，大二软件工程专业，考试周焦虑症资深患者 🤯。 |
| 目标 | ① 按课程名快速找历年真题 / 复习笔记；② 收藏有用的资源考试前再看；③ 把自己整理的笔记分享出去（获得小骄傲）。 |
| 痛点 | 找资料要跑 QQ 群 + 打印店 + 网盘，花 2 小时还找到的是旧版。 |
| 核心诉求 | **关键词 + 课程双条件搜索**；**收藏夹随时找得到**；**下载按钮"一次点击"就下成功**。 |

---

## 3. 角色用例矩阵（Role × Use Case Matrix）

> **符号说明**：✅ = 可执行；— = 不允许；⚠️ = 仅对自己的资源执行。

| # | 用例（用户任务） | 业务需求 BR 映射 | 管理员（Admin） | 教师（Teacher） | 学生（Student） | 匿名游客 |
|---|---|---|---|---|---|---|
| U-1 | 注册账号 | BR-A01 | — | ✅ 工号 | ✅ 学号 | ✅ |
| U-2 | 登录账号 | BR-A03/A06 | ✅ Admin | ✅ 工号 | ✅ 学号 | — |
| U-3 | 查看/修改个人信息（含头像） | BR-U01 | ✅ | ✅ | ✅ | — |
| U-4 | 修改自己的密码 | BR-U02 | ✅ | ✅ | ✅ | — |
| U-5 | 查看我的资源（分页/按状态筛选） | BR-R05 | ✅ 全部 | ✅ 自己的 | ✅ 自己的 | — |
| U-6 | 发布资源 | BR-R01/R02 | ✅（默认已通过？——不，规则一致：待审核） | ✅ ⚠️ | ✅ ⚠️ | — |
| U-7 | 修改资源 | BR-R02/R05 | ✅ 全部 | ✅ ⚠️ 自己的 | ✅ ⚠️ 自己的 | — |
| U-8 | 删除资源 | BR-R05 | ✅ 全部 | ✅ ⚠️ 自己的 | ✅ ⚠️ 自己的 | — |
| U-9 | 审核资源（通过 / 拒绝+理由） | BR-R02/AD02 | ✅ | — | — | — |
| U-10 | 搜索资源（关键词+课程+时间范围） | BR-R03 | ✅ | ✅ | ✅ | ✅（匿名） |
| U-11 | 查看资源详情 | BR-R06 | ✅ 全状态 | ✅ 已通过+自己的 | ✅ 已通过+自己的 | ✅ 仅已通过 |
| U-12 | 下载资源 | BR-R04/FI04 | ✅ 全状态 | ✅ 已通过+自己的 | ✅ 已通过+自己的 | —（需登录） |
| U-13 | 收藏 / 取消收藏资源 | BR-F01 | — | ✅ | ✅ | — |
| U-14 | 查看我的收藏列表 | BR-F02 | — | ✅ | ✅ | — |
| U-15 | 用户管理（增删改查） | BR-AD01 | ✅ | — | — | — |
| U-16 | 重置任意用户密码 | BR-AD01 | ✅ | — | — | — |
| U-17 | Excel 批量导入师生 | BR-AD04/05 | ✅ | — | — | — |
| U-18 | 资源管理（按上传者/状态过滤 CRUD） | BR-AD03 | ✅ | — | — | — |

---

## 4. 关键用例展开（Use Case Detail）

> 选取业务流中**高频 / 关键 / 易出错**的 7 个用例展开，覆盖从注册 → 发布 → 审核 → 检索 → 下载 → 收藏 → 批量导入的**完整闭环**。其余用例见矩阵行，逻辑类似。

---

### UC-001：用户注册（U-1）

| 项 | 内容 |
|---|---|
| **参与者** | 教师（工号）、学生（学号）、匿名游客（首次访问） |
| **前置条件** | ① 平台已启动；② 用户访问 `/register` 页 |
| **触发事件** | 用户点击「立即注册 →」进入注册页 |
| **基本流（成功路径）** | ① 选择身份：教师 / 学生；② 输入工号 / 学号、姓名、院系、专业（学生必填）、手机号（选填）、邮箱（选填）；③ 输入密码（≥8 位且含数字+字母）+ 确认密码；④ 点击「注册」；⑤ 系统校验→密码格式通过→查重通过→MD5 加密入库；⑥ 返回 `code=1`；⑦ 自动跳转 /login 登录页，提示「注册成功，请登录」。 |
| **异常流 1：账号已存在（Excel 已导入）** | ④ 提交后查重失败 → 返回 `code=2001 message=账号已存在，请直接登录` → 停留在注册页，保留其他字段内容。 |
| **异常流 2：密码格式不对** | 前端阻止提交 + 红框提示「密码需不少于 8 位且包含数字和字母」；后端兜底 `code=2004`。 |
| **异常流 3：两次密码不一致** | 前端 `el-form` 校验失败，不允许提交。 |
| **后置条件（成功）** | `user` 表新增 1 条记录，`role=1/2`，`status=0 正常`。 |
| **验证方法** | 用学号 `20260001` 注册 → 再注册同 1 个 → 得到 2001（冒烟脚本场景 2 已验证）。 |

---

### UC-002：用户登录（U-2）

| 项 | 内容 |
|---|---|
| **参与者** | 所有角色（Admin/Teacher/Student） |
| **前置条件** | 已存在合法账号（预置 Admin、Excel 导入、或 UC-001 注册成功） |
| **基本流** | ① 访问 `/login`；② 输入 `Admin` + `Admin2026`；③ 点击「登录」；④ 后端校验账号存在+MD5 匹配+未锁定；⑤ 返回 `{code=1, data={token, userInfo{id,role,name,department,...}}}`；⑥ Pinia `userStore` 写入 token+userInfo，localStorage 持久化；⑦ 路由守卫判断有 token → 跳 `/dashboard` 首页。 |
| **异常流 1：密码错误 ≤4 次** | 返回 `code=2002 账号或密码错误`，`login_fail_count +1`。 |
| **异常流 2：连续错误 5 次** | 第 5 次失败后 → `status=1 锁定`、`lock_time=NOW()` → 返回 `2003 账号已锁定，请稍后再试`，30min 内无法再登录。 |
| **异常流 3：未携带 Token 访问受保护页** | 路由守卫跳转 `/login` + 后端接口返回 1001。 |
| **后置条件（成功）** | 后续所有请求 `request.js` 自动加 `Authorization: Bearer <token>`。 |
| **验证方法** | 冒烟脚本 Smoke-①（PASS）+ UI 浏览器 真实点击（PASS）。 |

---

### UC-003：发布资源（U-6）

| 项 | 内容 |
|---|---|
| **参与者** | 教师、学生（登录后） |
| **前置条件** | ① 已登录（UC-002）；② 至少有 1 个文件已先完成上传（FI-UC001）。 |
| **触发事件** | 侧边栏「资源中心 → 发布资源」点进 `/resource/publish` |
| **基本流** | ① 填写资源标题（必填）、描述（选填）、所属课程（必填，如「软件工程」）；② 点击「选择文件」→ 弹出 FileUpload 组件 → 选本地 `.docx/.pdf` → 上传成功返回 `{fileStorageId, fileName, fileSize, hitQuickUpload, ...}`；③ 点击「提交」→ `POST /resource/publish` 带 6 字段或 fileStorageId；④ Service 回填 6 字段 / 校验必填 → `reviewStatus=0 待审核` 入库；⑤ 返回 `code=1 data={resourceId}`；⑥ Toast「发布成功，等待管理员审核」+ 跳转 `/profile/my-resources` 我的资源列表。 |
| **异常流 1：标题 / 课程为空** | 前端 `el-form` 拦截，红框提示；后端兜底 400 `PARAM_ERROR`。 |
| **异常流 2：未登录直接调接口** | JwtInterceptor 返回 `1001`。 |
| **后置条件** | `resource` 表 1 条记录；`review_status=0`，仅上传者本人 / Admin 在列表 / 详情页可见。 |

---

### UC-004：管理员审核资源（U-9）

| 项 | 内容 |
|---|---|
| **参与者** | Admin（林老师） |
| **前置条件** | ① 管理员已登录；② 至少 1 条资源处于 `待审核`（UC-003 发布成功）。 |
| **基本流：通过** | ① 菜单「管理后台 → 资源审核」进入 `/admin/resources/review`；② 筛选条件 `状态=待审核` 列出待审列表；③ 点击某一行「查看详情」（标题/课程/上传者/附件）；④ 点击「通过」→ `POST /admin/resources/review {resourceId, approve=true}`；⑤ Service 更新 `review_status=1, reviewer_id=<uid>, review_time=NOW()`；⑥ 返回 code=1，Toast「已通过，资源已公开发布」。 |
| **备选流：拒绝** | ④ 点击「拒绝」→ 弹出「请填写拒绝理由」对话框，rejectReason 必填；→ `approve=false, rejectReason="内容不规范"`；→ 写库 `review_status=2, reject_reason=xx`。 |
| **异常流：拒绝没填理由** | 前端对话框禁止提交；后端 DTO 自定义校验 `isRejectReasonMissing()` 返回 `PARAM_ERROR code=2`。 |
| **后置条件（通过）** | 匿名用户 / 其他师生都能在资源列表看到该资源（BR-R06 可见性规则满足）。 |
| **验证方法** | 冒烟脚本 Smoke-③（PASS）。 |

---

### UC-005：资源检索 + 下载（U-10 + U-12）

| 项 | 内容 |
|---|---|
| **参与者** | 师生（登录）、匿名游客（搜索可，下载需登录） |
| **基本流：搜索（匿名也可）** | ① 侧边栏「资源中心 → 资源列表」→ `/resource/list`；② 关键词填「数据结构」+ 课程下拉选「数据结构」+ 时间起止「2026-01-01 ~ 2026-08-26」；③ 点「搜索」→ `GET /resource/list keyword=数据结构&course=数据结构&startTime=xxx&endTime=xxx&pageNum=1&pageSize=10`；④ Service 过滤 `review_status=1（已通过）` + 动态 SQL + `ORDER BY create_time DESC`；⑤ 返回 PageResult `{list, total, pageNum, pageSize}`；⑥ 渲染 `<ResourceCard>` 卡片列表（标题、课程、下载数、收藏按钮、进入详情）。 |
| **基本流：下载（需登录）** | ④ 在详情页点击「下载附件」→ `GET /file/download/{resourceId}`（**注意：PathVariable 传资源 ID 不是文件 ID**）；⑤ Service 先校验 `review_status=1 OR 上传者本人 OR admin`；通过 → 原子 SQL `download_count = COALESCE(download_count,0)+1`；⑥ 解析路径 → 响应 200 + `Content-Disposition attachment; filename=xxx.pdf` → 浏览器自动下载。 |
| **异常流 1：下载未审核资源（他人）** | 返回 `3002 资源状态不允许该操作`。 |
| **异常流 2：文件已从磁盘删除（极端）** | 返回 `5005 文件不存在`。 |
| **后置条件** | `resource.download_count` 至少 +1（行锁级原子 SQL，并发安全）。 |
| **验证方法** | 冒烟脚本 Smoke-④ HTTP 200 + SQL rows>0 已执行 ✅。 |

---

### UC-006：收藏 / 取消收藏 + 我的收藏（U-13 / U-14）

| 项 | 内容 |
|---|---|
| **参与者** | 登录师生 |
| **基本流：收藏（Add）** | ① 资源详情页 ⭐ 图标「收藏」→ `POST /favorite/add body={resourceId}`；② Service 先查 unique `(user_id, resource_id)` 已存在 → 返回 `existed=true, favorited=false` 幂等；不存在 → `INSERT favorite(create_time=NOW())` → 返回 `favorited=true`；③ 图标状态反转。 |
| **基本流：取消收藏（Remove）** | ⭐ 再次点击 → `POST /favorite/remove body={resourceId}` → `DELETE FROM favorite WHERE user_id=? AND resource_id=?` → Toast「已取消收藏」。 |
| **基本流：我的收藏列表** | 侧边栏「个人中心 → 我的收藏」→ 分页返回**已通过 + 被我收藏**的资源（含标题/课程/上传者/收藏时间）→ 可二次关键词过滤。 |
| **异常流：resourceId 无效** | 返回 `3001 资源不存在`。 |
| **验证方法** | 冒烟脚本 Smoke-⑤ （Add→Remove→Re-add 三次 code=1 ✅）。 |

---

### UC-007：管理员 Excel 批量导入（U-17）

| 项 | 内容 |
|---|---|
| **参与者** | Admin（林老师） |
| **前置条件** | ① 已准备好 500 学生的 `.xlsx`；② 表头：学号 / 姓名 / 手机 / 邮箱 / 院系 / 专业。 |
| **基本流** | ① 管理后台 → 批量导入 → `/admin/users/import`；② 选择 `.xlsx` 文件 → `POST /admin/users/import`（Multipart 表单）；③ `ExcelUtil.parseExcel` 读取 Sheet；④ 每一行：格式校验 → 必填校验 → 密码默认校验（PRD 初始密码规则）→ 查重；⑤ 通过行批量 insert 进 user 表（MD5 加密密码，默认 `role=2 学生`）；⑥ 返回 `ImportResultResponse {total, successCount, failCount, rowErrors:[{rowIndex, field, message}]}`；⑦ `<ImportResult>` 组件展示成功几条 / 失败几条，失败的具体哪一行哪一列错了（如「第 23 行：学号为空」）。 |
| **异常流 1：上传的是 .csv / .xls 老格式** | 校验失败 → `4001 Excel 格式错误`，只接受 `.xlsx`。 |
| **异常流 2：第 23 行学号为空** | 跳过该行，加入 rowErrors；其他 499 行继续入库（MVP：行级错误不阻塞合法行，见 BR-AD05）。 |
| **后置条件** | `user` 表批量新增 N 条记录；导入结果给到明细可下载 / 可截图存档。 |

---

## 5. 用户非操作型需求（User Experience Constraints）

> 以下是用户（尤其是最终学生/教师）对"好不好用"的非功能性诉求，由 UX + 前端共同保障：

| # | UX 需求 | 用户预期 | 系统实现方式 |
|---|---|---|---|
| UX-01 | **快速上手**：零学习成本，学生第一次进来能在 30 秒内找到「高级语言程序设计」的复习资料 | 首页「快捷入口 → 全部资源」直接进入资源列表；顶部搜索框 1 处；卡片大、易点击 | Dashboard 快捷入口 + ResourceFilter 组件（关键词+课程+时间三控件一行排布） |
| UX-02 | **反馈明确**：操作有 toast；失败有原因 | 例如下载被拒绝，不要只显示「失败」，要显示「仅已通过资源可下载」 | Element Plus `ElMessage` + 后端统一 message（所有错误码有标准提示，见 ResponseCodeEnum） |
| UX-03 | **移动端友好（MVP 基础适配）** | 用手机查资料也要能看 | Element Plus 栅格 + 视口 694px 下正常渲染（本次 Smoke-4 snapshot 在 694×768 正常） |
| UX-04 | **加载不白屏** | 切换页面有 loading，不丢用户上下文 | `Pinia appStore` loading 态 + `<Pagination>` 稳定分页条 + `<EmptyState>` 空状态插画 |
| UX-05 | **可见即所得的角色过滤** | 学生看不到"资源审核"四个字；避免"点进去才被拒绝" | 侧边栏 `Sidebar.vue` 遍历 routes 时按 `meta.roles` 过滤（用户看不到=不会点=无挫败） |
| UX-06 | **中文绝对正常** | 所有姓名、院系、拒绝理由、资源标题 100% 显示正确，**绝对不能出现乱码方块或「锟斤拷」** | utf8mb4 三层对齐（init.sql → Hikari init SQL → JDBC UTF-8），见业务约束 BC-02 ✅（已字节级验证） |

---

## 6. 用户验收标准（User Acceptance Tests, UAT）

> UAT 以学生小李为主人公跑一天的典型使用故事（User Story）。

| UAT # | 典型用户故事 | 验收通过 = 完成全部步骤且全程无异常 | 结果（2026-08-26） |
|---|---|---|---|
| UAT-1 | **作为学生，我要找《软件工程》的真题复习** | 注册 → 登录 → 资源列表搜「软件工程」→ 能看到 Admin 审核通过的 Smoke Resource → 进入详情 → 下载成功 → 下载数 +1 ✅ | ⭕ 脚本级通过（UI 层列表待真数据对接） |
| UAT-2 | **作为学生，我要把自己整理的笔记分享出去** | 我的资源 → 发布 → 上传「我的笔记.txt」→ 提示「等待审核」→ 我的资源 Tab「待审核」能看到它 → 之后 Admin 通过 → 再次搜课程能搜出来 | ✅ API 级（smoke Smoke-②+③） |
| UAT-3 | **作为教师，我要发布课件** | 教师注册 → 上传「软件工程 Chapter1.pptx」→ 发布 → 待审核 → Admin 通过 → 列表能被其他学生看到 → 课件被下载 5 次，下载数=5 | ✅ API 级 |
| UAT-4 | **作为管理员，我要把新的 1 个学生 Excel 批量导入** | 导入 1 行 Excel → 结果 successCount=1 failCount=0 → 登录这个新学生账号 → 正常登录 → 名字 / 院系正确 → 个人中心可显示 | ✅ Service 级（AdminServiceImpl 有完整单元级校验链路） |
| UAT-5 | **暴力破解保护**：恶意用户 5 次输错密码后再用正确密码也登不上 | 连续 5 次 Admin/错误密码 → 第 5 次看到「账号已锁定」→ 30min 内用正确密码也登不上 → 30min 后自动解锁成功 | ⭕ MVP 骨架已实现（User 表字段 + ResponseCodeEnum 2003，阈值常量 5/30min 待 Service 层精确实现） |

---

## 7. 用户需求 → 系统需求映射矩阵（Traceability）

| 用户用例 | → | 系统功能需求（系统需求文档 §3） |
|---|---|---|
| UC-001 注册 | → | FR-A01 注册双通道、FR-A02 密码校验、FR-A04 MD5 加密入库 |
| UC-002 登录 / 锁定 | → | FR-A05 登录 + FR-A06 登录失败计数 + FR-A07 JWT 签发 |
| UC-003 发布资源 | → | FR-R01 资源 CRUD + FR-FI01 上传 + FR-FI03 MD5 秒传 |
| UC-004 审核 | → | FR-AD01 资源审核（通过/拒绝+理由）+ 状态回退 |
| UC-005 检索 + 下载 | → | FR-R02 列表分页搜索 + FR-R03 详情 + FR-R04 原子下载计数 + FR-FI02 下载 |
| UC-006 收藏 | → | FR-F01 收藏幂等 + FR-F02 我的收藏列表 |
| UC-007 Excel 导入 | → | FR-AD02 Excel 导入 + 行级错误 + 事务安全 |
| 管理台增删改查密码 | → | FR-AD03 用户管理 + FR-AD04 重置密码 |
| UX-01~UX-06 | → | 系统需求 §4 非功能需求（NFR-01~NFR-12） |
