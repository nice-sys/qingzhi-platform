# 青知共享平台 · 需求分析文档——业务需求

> 文档版本：v1.0
> 整理日期：2026 年 8 月
> 文档类型：**需求分析 — 业务需求（Business Requirement）**
> 关联文档：[PRD_青知共享平台.md](../PRD_青知共享平台.md)、[Part.md](../Part.md)

---

## 1. 文档定位与阅读指引

| 维度 | 说明 |
|---|---|
| **文档目的** | 从**建设方 / 业务方视角**，阐明本平台"为什么要建、要建成什么样、为哪些业务场景服务、最终交付的业务目标和成功标准"。 |
| **读者对象** | 项目发起人、业务负责人、产品经理、项目经理、开发团队、测试团队、高校信息化部门。 |
| **对应开发交付物** | 后端 Spring Boot 工程 `backend/` + 前端 Vue 3 工程 `frontend/` + 初始化 SQL `backend/sql/init.sql` + 冒烟脚本 `smoke.ps1`。 |
| **验证基线** | 本文件 2~5 节的每条业务需求，均需在「用户需求」「系统需求」中逐层展开，并由冒烟脚本 7/7 PASS 验收闭环。 |

---

## 2. 项目背景与业务动机（Business Context）

### 2.1 现实痛点

- **P1：资源获取低效，考试周焦虑显著。**
  考试周期间学生往往需要在**微信群、打印店、QQ 群、零散笔记、学长学姐网盘**等多个渠道中零散地搜集复习资料，耗时久、资料版本参差、质量无法保证。
- **P2：优质资源沉淀不足，缺少统一组织。**
  教师课件、课程 PPT、历年真题、实验报告、学习笔记等"学习资产"长期散落在个人设备中，**没有统一的分类（按课程）、审核（保障质量）、检索（按关键词/时间）与复用机制**。
- **P3：共享激励缺位。**
  发布资料无统一入口、教师/学生贡献者缺乏可见的"贡献展示（我的资源 / 下载数 / 收藏数）"，导致共享意愿低。
- **P4：账号体系与权限管控缺失。**
  高校内既有「老师-学生-管理员」三角色，又有"批量导入"与"自行注册"两种账号来源。若没有统一的角色权限体系和审核机制，易出现：
  - 错误 / 过时资料被公开展示；
  - 非授权人员修改 / 删除他人资源；
  - 外部无关人员随意访问内部学习资料。

### 2.2 业务目标（Business Objectives）

| 编号 | 业务目标 | 可度量的成功标准 | 对应 PRD 第 1.2 节 |
|---|---|---|---|
| BO-01 | **构建统一在线资源共享系统** | 支持师生发布、查找、收藏学习资源；管理员进行资源审核与用户管理。 | 核心目标 |
| BO-02 | **账号双通道并存** | 批量导入（Excel）与自行注册双通道并行，注册失败时返回「账号已存在请直接登录」等可读提示。 | 认证目标 |
| BO-03 | **角色权限差异化** | 管理员 / 教师 / 学生三者权限矩阵清晰；非授权操作返回 1002 无权限；登录失败跳转登录页。 | 权限目标 |
| BO-04 | **资源生命周期全闭环** | 发布 → 审核 → 列表 / 详情 → 下载计数 → 收藏 → 修改 / 删除，所有状态流转有记录、可追溯。 | 资源目标 |
| BO-05 | **平台质量与安全基线** | 统一响应格式、全局异常捕获、密码加密、JWT 鉴权；敏感操作鉴权覆盖率 100%。 | 质量目标 |

---

## 3. 业务范围（Business Scope）

### 3.1 业务范围矩阵

| 业务域 | 纳入范围（In Scope） | 不纳入范围（Out of Scope） |
|---|---|---|
| **认证与用户** | 注册、登录、密码格式校验、个人信息修改、头像、密码修改、登录锁定（防暴力破解）、管理员重置密码 | 短信 / 邮箱验证码登录、OIDC / 微信 / 钉钉 SSO 单点登录、忘记密码自助找回 |
| **角色与权限** | 三角色（管理员 / 教师 / 学生）RBAC 权限矩阵、接口级 1002 拒绝、前端路由守卫按 `meta.roles` 过滤 | 动态自定义角色、行级数据权限（按院系）、ABAC 策略引擎 |
| **资源管理** | 资源发布、修改、删除；审核流（待审核/已通过/已拒绝）；按关键词+课程+时间范围分页查询；详情页；下载原子计数 | 在线预览、批量修改资源标签、资源评分 / 评论 / 举报、资源版本管理、资源打赏 / 积分 |
| **收藏体系** | 收藏 / 取消收藏；我的收藏列表（含分页+关键词筛选）；资源详情页展示是否已收藏 | 收藏夹分组、共享收藏夹 |
| **文件能力** | 单文件 ≤50MB 上传；MD5 哈希秒传（文件去重）；按 ResourceId 下载；上传限流 6 次/分 | 对象存储 S3 接入、CDN 加速、文件在线转换、分片上传 |
| **管理后台** | 用户增删改查、强制重置密码；资源审核（通过/拒绝+理由）；资源管理 CRUD；Excel 批量导入（行级错误回显+汇总） | 操作审计日志、数据大屏可视化报表、系统参数配置 |

### 3.2 业务角色与业务用例概览

| 角色 | 主要业务活动 | 业务目标 |
|---|---|---|
| **管理员（Admin）** | 导入师生账号 → 审核资源 → 管理用户 → 重置密码 → 管理资源 | BO-02~05：保障平台合规、安全、高质量运行 |
| **教师（Teacher）** | 注册/登录 → 发布课件 → 修改自己的资源 → 查看下载数据 → 收藏资料 | BO-01/04：沉淀课程资产、复用优质资料 |
| **学生（Student）** | 注册/登录 → 按课程/关键词搜索资源 → 下载复习资料 → 收藏 → 分享自己的笔记 | BO-01/04：高效获取资料、贡献学习笔记 |

### 3.3 核心业务流程图（文字版 — 审核闭环）

```
学生/教师 发布资源 → [待审核] only admin&self 可见
      │
      ▼
管理员 列表筛选「待审核」→ 查看详情：
   ├─ 通过（approve=true）  →  [已通过] 所有人可见 + 进入资源列表
   └─ 拒绝（approve=false + rejectReason 必填）→ [已拒绝] 仅 self/admin 可见，学生可修改后再次提交 → [待审核]
```

---

## 4. 业务需求清单（Business Requirements Catalog）

> 每条业务需求（BR）都将在「用户需求文档」中分解为用户任务（User Task），在「系统需求文档」中细化为功能/非功能需求（FR/NFR）。

### 4.1 认证与账号域

| 编号 | 业务需求 | 业务规则（Business Rules） | 关联成功标准 |
|---|---|---|---|
| BR-A01 | **双通道账号创建** | ① 管理员可用 Excel 批量导入师生；② 师生可自行用学号/工号注册；③ 两通道合并查重，重复返回"账号已存在"。 | BO-02 |
| BR-A02 | **密码强度统一合规** | 密码长度 ≥8 位且必须包含**数字 + 字母**；注册/改密/导入初始密码**前后端双重校验**。 | BO-05 |
| BR-A03 | **管理员预置账号** | 系统初始化后必须可使用 `Admin / Admin2026` 登录，不可自行注册。 | BO-03 |
| BR-A04 | **密码加密存储** | 数据库**仅存储 MD5 加密后的密码摘要**，任何接口不得返回明文密码。 | BO-05 |
| BR-A05 | **登录防护（防暴力破解）** | 同一账号连续失败 5 次后锁定 30 分钟；锁定期间无论密码对错均返回"账号已锁定"。 | BO-05 |
| BR-A06 | **JWT 令牌鉴权** | 登录成功后颁发 JWT；除白名单（登录/注册/公开列表/下载等）外，其余接口必须携带有效 JWT，否则返回 1001。 | BO-03/05 |

### 4.2 角色与权限域

| 编号 | 业务需求 | 业务规则（权限矩阵摘要） | 关联成功标准 |
|---|---|---|---|
| BR-P01 | **RBAC 三角色** | 管理员（0）/ 教师（1）/ 学生（2）三角色不可越权；权限矩阵严格按 PRD §2.2.1 执行。 | BO-03 |
| BR-P02 | **前端菜单按角色过滤** | 「管理后台」菜单仅 Admin 可见；教师/学生不显示「资源审核、用户管理」。 | BO-03 |
| BR-P03 | **后端兜底鉴权** | 所有管理后台接口在后端 Service 层做角色校验，不依赖前端隐藏菜单，越权一律 1002。 | BO-03/05 |

### 4.3 资源生命周期域

| 编号 | 业务需求 | 业务规则 | 关联成功标准 |
|---|---|---|---|
| BR-R01 | **资源发布必填校验** | 标题、所属课程、关联文件 3 项必填；缺少任一项返回参数错误。 | BO-01/04 |
| BR-R02 | **资源审核流转** | ① 提交 → 待审核（仅本人+管理员可见）；② 管理员通过 → 已通过（所有人可见）；③ 管理员拒绝 → 已拒绝（需理由）；④ 修改已通过 → 再次进入待审核。 | BO-04 |
| BR-R03 | **资源可检索** | 按关键词（标题+描述）、所属课程、**发布起止日期**过滤；按发布时间**倒序**分页返回。 | BO-04 |
| BR-R04 | **下载原子计数** | 每次有效下载（= 权限校验通过 + 文件可落盘）**在数据库行级原子自增 `download_count = COALESCE(dc,0)+1`**，并发不丢更新。 | BO-04 |
| BR-R05 | **仅资源所有者可修改/删除自己的资源** | 他人修改返回 1002 / 3002，管理员对任意资源可增删改查。 | BO-03/04 |
| BR-R06 | **未通过资源可见性** | 待审核 / 已拒绝资源只对**上传者本人 + 管理员**可见；匿名用户 / 其他用户访问返回 3001 / 3002。 | BO-03/04 |

### 4.4 收藏与个人中心域

| 编号 | 业务需求 | 业务规则 | 关联成功标准 |
|---|---|---|---|
| BR-F01 | **收藏防重** | 同一用户对同一资源只能收藏一次；重复调用接口幂等返回"已收藏"。 | BO-01/04 |
| BR-F02 | **我的收藏可分页 / 可搜索** | 我的收藏列表支持分页、关键词过滤，按收藏时间倒序。 | BO-04 |
| BR-U01 | **个人信息可维护** | 用户可补充 / 修改：姓名、手机、邮箱、院系、专业、头像；但学号/工号不可修改。 | BO-01/02 |
| BR-U02 | **修改密码需校验旧密码** | 旧密码不匹配不得修改；新密码必须符合密码规则。 | BO-05 |

### 4.5 文件能力域

| 编号 | 业务需求 | 业务规则 | 关联成功标准 |
|---|---|---|---|
| BR-FI01 | **单文件 ≤50MB** | 超限即返回 5001，不占用服务器存储。 | BO-05 |
| BR-FI02 | **支持常见格式** | PDF / DOC / DOCX / PPT / PPTX / XLS / XLSX / PNG / JPG / TXT / ZIP / RAR 等常见文件类型。 | BO-01 |
| BR-FI03 | **文件秒传（去重）** | 上传前计算 MD5 文件哈希，若 `file_storage` 中已存在同哈希记录 → 直接复用原文件、不重复落盘、引用计数 +1。 | BO-04/05（存储成本） |
| BR-FI04 | **按资源 ID 下载** | 下载入口 PathVariable 传 resourceId（非 fileStorageId）；仅审核通过资源允许下载。 | BO-04 |
| BR-FI05 | **上传限流（防刷）** | 同一用户 60 秒窗口内最多上传 6 个文件；超窗口返回 5003。 | BO-05 |

### 4.6 管理后台域

| 编号 | 业务需求 | 业务规则 | 关联成功标准 |
|---|---|---|---|
| BR-AD01 | **用户 CRUD + 重置密码** | 管理员可增删改查所有用户；重置密码功能无需提供旧密码，重置后返回新密码（建议强制改密，MVP 不强制）。 | BO-02/03 |
| BR-AD02 | **资源审核（通过 / 拒绝）** | 拒绝时 rejectReason 必填；通过 / 拒绝均需记录审核人 ID、操作时间。 | BO-03/04 |
| BR-AD03 | **资源管理** | 管理员可按审核状态、上传者、关键词过滤，可删除不当资源。 | BO-03/04 |
| BR-AD04 | **Excel 批量导入** | 仅 .xlsx；表头严格按"学号/工号、姓名、手机、邮箱、院系、专业（仅学生）"顺序；失败时逐行返回错误原因 + 成功/失败统计。 | BO-02/05 |
| BR-AD05 | **导入事务安全** | 一行失败不应影响其他合法行；导入结果提供成功条数 + 失败条数 + 失败行明细。 | BO-02/05 |

---

## 5. 业务约束（Business Constraints）

| 编号 | 约束项 | 约束说明 | 影响范围 |
|---|---|---|---|
| BC-01 | **技术栈强约束** | 后端必须 Spring Boot + MyBatis 纯 XML + Maven；前端必须 Vue 3 + Vite + Element Plus。（见 PRD §3.3） | 技术选型 |
| BC-02 | **数据库强约束** | 必须 MySQL；数据库名 `qingzhi_platform`；字符集 `utf8mb4_0900_ai_ci` 三层对齐（SQL BOM + Hikari init-SQL + JDBC `UTF-8`），杜绝 Windows 1366。 | 建库与部署 |
| BC-03 | **JDK / 编译目标约束** | Maven `maven-compiler` release=17，可在 JDK 17 / 21 / 26 上运行。 | 构建与部署 |
| BC-04 | **统一响应契约** | 所有 API 必须返回 `{code, message, data}` 三段式；code=1 成功，其余失败，错误码枚举严格按 PRD §5.3。 | API 设计 |
| BC-05 | **公开资料可匿名访问** | 资源列表 / 资源详情 / 字典接口对未登录用户**匿名可读**（不抛 1001），但所有写操作（发布、收藏、下载）必须登录。 | 交互体验 |
| BC-06 | **MVP 不做在线预览** | 在线预览（POI→PDF / OpenOffice）作为 PRD 加分项「待实现」，MVP 版本默认不提供。 | 范围 |
| BC-07 | **不做支付 / 积分体系** | 所有资源免费下载，不引入积分、VIP、打赏、广告。 | 商业模式 |

---

## 6. 业务成功标准（Business Success Criteria / Acceptance）

> 以下 4 条全部满足，方可判定业务需求交付完成。

| # | 成功标准 | 验收方式 | 当前状态（2026-08-26） |
|---|---|---|---|
| S-01 | **核心业务链路 7/7 场景通过**（Admin登录、学生注册登录、上传发布、审核通过、下载计数、收藏切换、秒传+限流5003） | 执行 `smoke.ps1` 一键脚本 → FINAL SUMMARY 全 PASS | ✅ PASS |
| S-02 | **角色权限无越权**：学生访问 `/admin/**` 一律 1002；匿名发布资源一律 1001；他人修改资源一律 1002/3002 | Postman / curl 负向用例 5 条 | ✅ 已由 JwtInterceptor + PermissionUtil 保障 |
| S-03 | **UTF-8 / utf8mb4 全链路中文正常**：姓名"超级管理员"、院系"系统管理" HEX 字节完全匹配；init.sql 中文插入无 ERROR 1366 | `SELECT HEX(name),HEX(department) FROM user WHERE id=1` 字节比对 | ✅ 字节验证通过 |
| S-04 | **浏览器 UI 登录-首页渲染闭环**：管理员登录跳转 Dashboard、显示"超级管理员 欢迎回来 + 统计卡 + 快捷入口"无控制台 Error | 浏览器实际打开 + snapshot + 控制台检查 | ✅ Dashboard 正确渲染（见 Smoke-4） |

---

## 7. 业务风险与缓解（Business Risks & Mitigation）

| 风险 | 可能性 | 影响 | 缓解措施（本项目已实现） |
|---|---|---|---|
| 发布错漏资料被公开 | 中 | 高 | **强制审核流**（待审核 → 已通过），不审核不公开；拒绝理由必填。 |
| 上传大文件占满磁盘 | 中 | 中 | **50MB 上限** + **MD5 秒传去重** + **上传限流 6/分钟**，三重保障。 |
| 账号被撞库 / 暴力破解 | 中 | 高 | 密码格式强校验 + 失败 5 次锁 30min + JWT 24h 过期 |
| 学生乱发布笔记 | 低 | 中 | 审核流 + 管理员资源管理 CRUD，可随时删除不当内容。 |
| Windows 部署字符集乱码 | 高（实际踩坑） | 高 | init.sql 加 UTF-8 BOM 头 + 8 条 SESSION `SET NAMES utf8mb4` + `--default-character-set=utf8mb4` 执行。 |
