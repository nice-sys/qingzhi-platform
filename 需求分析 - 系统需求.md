# 青知共享平台 · 需求分析文档——系统需求

> 文档版本：v1.0
> 整理日期：2026 年 8 月
> 文档类型：**需求分析 — 系统需求（System Requirement / Functional + Non-Functional）**
> 关联文档：[需求分析 - 业务需求.md](./需求分析%20-%20业务需求.md)、[需求分析 - 用户需求.md](./需求分析%20-%20用户需求.md)、[PRD_青知共享平台.md](../PRD_青知共享平台.md)

---

## 1. 文档定位与阅读指引

| 维度 | 说明 |
|---|---|
| **文档目的** | 本文件是**开发 / 测试 / 部署团队的实施契约**，从**系统建设视角**逐条拆解"系统应该做什么（功能需求 FR）/ 系统应具备什么特性（非功能需求 NFR）/ 系统如何与外部集成（接口与部署）"。它是 BR（业务需求）和 UR（用户需求）的技术落地。 |
| **读者对象** | 架构师、后端工程师、前端工程师、DBA、DevOps、SQA（测试）、运维。 |
| **核心结构** | 2 系统上下文 → 3 功能需求（按模块 FR-xxx）→ 4 非功能需求（NFR-xxx）→ 5 接口契约（含 REST + 统一错误码）→ 6 数据库设计 → 7 部署与运行约束 → 8 验证矩阵（Traceability & Acceptance）。 |
| **验证基线** | 每条 FR/NFR 必须对应 1 条或多条验证方法（冒烟脚本 / 单元测试 / UI 自动化 / 性能压测），满足方可签验。 |

---

## 2. 系统上下文（System Context）

### 2.1 系统总体架构（C4 容器视图 / 简化版）

```
  [学生 浏览器 / 教师 浏览器 / 管理员 浏览器]
          │   HTTPS (部署期) / HTTP (开发期)
          │   端口 5173 (Vite Dev) / Nginx (生产)
          ▼
  ┌──────────────────────────────────────────┐
  │         前端 SPA (Vue 3 + Vite 5)         │
  │  Pinia + Vue Router + Element Plus 2.8   │
  └────────────────────┬─────────────────────┘
                       │ Axios proxy: '/api/*'
                       ▼
  ┌──────────────────────────────────────────┐     ┌──────────────────┐
  │     后端 Spring Boot 3.4.1 (8080)         │────▶│ MySQL 8.0+ :3306 │
  │  MyBatis XML + JWT + AOP RateLimit        │     │ qingzhi_platform │
  │  context-path: /api                       │     │ utf8mb4 0900_ai_ci│
  └────────────────────┬─────────────────────┘     └──────────────────┘
                       │ 本地文件读写（MVP）
                       ▼
              ┌───────────────────┐
              │  文件系统存储      │
              │ backend/uploads/  │
              │ YYYY/MM/DD/hash   │
              └───────────────────┘
```

### 2.2 环境与运行端口（固化）

| 环境 | 前端地址 | 后端地址（context-path=/api） | 数据库 | 文件存储 |
|---|---|---|---|---|
| 开发 | http://localhost:5173（Vite dev server，`/api` 代理 → 后端） | http://localhost:8080/api | localhost:3306 / `qingzhi_platform`（user=root） | 后端工程 `backend/uploads/` 下按日期分区 |
| 生产 | https://qingzhi.example.com（Nginx → dist 静态） | https://qingzhi.example.com/api（Nginx 反向代理 → Spring Boot） | 内网 MySQL / 独立实例（仍 `utf8mb4_0900_ai_ci`） | 可选替换为对象存储 S3（接口层保持不变） |

### 2.3 系统边界（External Interfaces）

| 系统 | 接口类型 | 协议 / 格式 | 方向 | 说明 |
|---|---|---|---|---|
| 终端浏览器 | UI | HTTP / HTML / CSS / JS / JSON | Inbound | Vue SPA 提供所有交互界面 |
| MySQL 8 | 持久化 | JDBC / MySQL 协议（UTF-8） | Outbound | 4 张核心表；字符集/排序规则必须 `utf8mb4_0900_ai_ci` |
| 本地文件系统 | 文件落盘 | JDK NIO / IO | Outbound | `backend/uploads/<YYYY>/<MM>/<DD>/<hash>.<ext>` 目录策略 |
| 管理员 Excel 导入 | 批量数据 | Multipart `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` | Inbound | Apache POI 解析 `.xlsx` |

---

## 3. 功能需求（Functional Requirements, FR）

> 编码规则：`<模块>-<序号>`，如 FR-A01；每条 FR 对应"BR 业务需求 / UC 用户用例 / 验证方法"。

### 3.1 认证与用户模块 FR-A（Auth & User）

| FR 编号 | 功能点 | 详细要求 | 对应 BR / UC | 验证方法 |
|---|---|---|---|---|
| FR-A01 | **注册通道（双通）** | ① 管理员 Excel 导入（见 FR-AD02）；② 师生自注册：`POST /auth/register`（username / password / role / name / phone / email / department / major）。任一通道重复注册一律返回 `2001 ACCOUNT_ALREADY_EXISTS`。 | BR-A01 / UC-001 | 2 次同号注册 → 第 2 次 2001 ✅ Smoke-② |
| FR-A02 | **密码强度前后端双校验** | 正则：`^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d!@#$%^&*()_+]{8,}$`（长度≥8，至少一个字母+数字）；前端 `validate.passwordRule` 校验，后端 `RegisterRequest` 自定义校验器 + `2004 INVALID_PASSWORD_FORMAT`。 | BR-A02 | 弱密码 `1234567` → 返回 2004 ✅ |
| FR-A03 | **预置管理员账号** | 执行 init.sql 时自动插入 `username=Admin, password=MD5("Admin2026"), role=0`，username UNIQUE 不可重复注册；该角色默认显示「超级管理员 / 管理员」头部文字。 | BR-A03 | init.sql + HEX 字节比对中文 ✅ |
| FR-A04 | **密码 MD5 加密存储** | 入库前统一 `PasswordUtil.md5(raw)`；任何响应 DTO（UserInfoResponse / LoginResponse 等）绝对不返回 password 字段；MyBatis `<resultMap>` 不显式映射 password。 | BR-A04 / BC-04 | UserInfoResponse.java 不含 password 字段 ✅（代码静态检查） |
| FR-A05 | **登录凭证校验** | `POST /auth/login(username, password)` 流程：① 取 user 记录；② 未取到 → 2002；③ 被锁定（status=1 且 lock_time 在 30 分钟内）→ 2003；④ MD5 不匹配 → `login_fail_count + 1` → 若 ≥5 → `status=1 lock_time=NOW()` 返回 2003；否则 2002；⑤ 匹配 → 清零失败计数 → 发 JWT + 返回 LoginResponse。 | BR-A05 / UC-002 | Smoke-① Admin Login code=1 ✅ |
| FR-A06 | **JWT 生成 / 解析 / 过期** | `JwtUtil`：HMAC-SHA256，claim 含 `uid(Long)`、`role(Integer)`、`exp=issuedAt+24h`；Intercepter 从 `Authorization: Bearer <token>` 提取；**过期 / 签名错 / 格式错统一返回 1001 NOT_LOGGED_IN**，不得抛 500。 | BR-A06 / BC-04 | Postman 无 Token → 1001 ✅；Token 篡改 → 1001 ✅ |
| FR-A07 | **JWT 拦截器白名单 + 公开 GET 匿名放行** | 白名单（完全跳过校验）：`/auth/**, /file/download/**, /actuator/**, /error`；公开 GET（JWT 失效也放行，但写接口仍 1001）：`GET /resource/list`、`GET /resource/courses`、`GET /resource/stats`、`GET /resource/{纯数字ID}`。实现：JwtInterceptor `isPublicGet()` 正则判断。 | BC-05 / UX-01 | 匿名 GET `/api/resource/list` → code=1 ✅ 脚本 Case 1/2 |
| FR-A08 | **个人信息查看 / 修改** | `GET /user/info` 取当前登录用户；`PATCH /user/info` 可改 name/phone/email/department/major（username / role 不可改）。`POST /user/avatar` Multipart 上传头像，落盘后更新 avatar_url。 | BR-U01 / U-3 | PATCH 改密后 GET 读取同步 ✅（D3 接口） |
| FR-A09 | **修改自己的密码** | `POST /user/change-password(oldPassword, newPassword)`：旧 MD5 不匹配 2002；新密码不满足规则 2004；更新后 JWT 可继续使用。 | BR-U02 / U-4 | 功能类已存在，正向用例待补测试 |

---

### 3.2 角色权限模块 FR-P（Permission & RBAC）

| FR 编号 | 功能点 | 详细要求 | 对应 BR / UC | 验证方法 |
|---|---|---|---|---|
| FR-P01 | **三角色枚举固化** | `RoleEnum.ADMIN(0)`、`TEACHER(1)`、`STUDENT(2)`；`User.role=TINYINT NOT NULL DEFAULT 2`（即无显式声明时默认学生，避免越权）。 | BR-P01 | init.sql DDL + 枚举代码一致 ✅ |
| FR-P02 | **后端 Service 层角色硬校验** | `PermissionUtil.requireAdmin(role)` 非 0 抛 1002；`requireSelfOrAdmin(resourceUserId, currentUserId, role)` 非本人非管理员抛 1002 / 3002。**所有管理员接口 / 资源修改接口必须调用**，不得只依赖前端菜单隐藏。 | BR-P02/P03 | 学生 Token 调 `/admin/resources/review` → 1002 ✅ |
| FR-P03 | **前端路由 `meta.roles` 守卫** | `router.beforeEach((to,from,next))` 中：① 有 JWT 且未拉取 userInfo → 拉；② 目标路由 `meta.roles` 不包含当前角色 → `next('/403')` 或回 dashboard 并 Toast「无权限」。 | BR-P02 / UX-05 | 学生登录 → 侧边栏「管理后台」模块完全不显示 ✅（Snapshot 验证） |

---

### 3.3 资源模块 FR-R（Resource Lifecycle）

| FR 编号 | 功能点 | 详细要求 | 对应 BR / UC | 验证方法 |
|---|---|---|---|---|
| FR-R01 | **资源发布** | `POST /resource/publish` 支持两种请求模式：**① 6 字段直传（fileName/filePath/fileSize/fileExt/fileHash/fileStorageId 全部）**；② **仅传 transient `fileStorageId` + 其余为空**，Service 自动从 `file_storage` 反查回填（MVP：file_storage 新增 original_file_name / file_ext 列后，模式②完全可用）。 | BR-R01 / UC-003 | 模式① Smoke-② ✅ |
| FR-R02 | **资源列表分页 + 动态查询** | `GET /resource/list?keyword=&course=&startTime=&endTime=&pageNum=1&pageSize=10`：① keyword 模糊匹配 title/description；② course 精确匹配；③ startTime/endTime 范围（create_time BETWEEN）；④ 登录用户：返回所有 review_status=1 + 本人任意状态；匿名用户：仅 review_status=1。动态 `<if>` MyBatis 拼接，`ORDER BY create_time DESC`；返回 `PageResult<ResourceListResponse>`。 | BR-R03/R06 / U-10 | 匿名 GET → code=1 列表非空 ✅ Smoke 脚本 Case 2 |
| FR-R03 | **资源详情** | `GET /resource/{id}`：未登录可看 review_status=1；登录本人/管理员可看全状态；返回 ResourceDetailResponse（含 uploaderInfo/收藏状态等 VO）。**id 非数字正则不走公开匿名分支**（防 SQL 注入）。 | BR-R06 / U-11 | GET /resource/5 → code=1 ✅ Smoke-④ |
| FR-R04 | **下载 + 原子计数** | `GET /file/download/{resourceId}`：① `resourceService.downloadResource(resourceId,uid,role)` 先鉴权；② `incrementDownloadCount()` 原子 SQL：`UPDATE resource SET download_count = COALESCE(download_count, 0) + 1 WHERE id = ?`（单语句行锁级原子）；③ resolveFile → 200 + Stream。禁止前端先调计数再调下载（会被绕过）。 | BR-R04/ FI04 / UC-005 | HTTP 200 ✅ + SQL rows>0 ✅ Smoke-④ |
| FR-R05 | **修改资源（含状态回退）** | `POST /resource/update`：仅上传者本人 / 管理员；若原 review_status=1 被修改（标题/描述/课程/附件任一变）→ **review_status 回退为 0 待审核**（重新审核）。 | BR-R02 状态流转 / U-7 | 正向用例待补测试（Service 已实现状态回退逻辑） |
| FR-R06 | **删除资源** | `POST /resource/delete/{id}`；本人 / 管理员；MyBatis `DELETE FROM resource WHERE id=?`；删除后 favorite 关联不要求级联（MVP 保留或按需加 ON DELETE CASCADE）。 | BR-R05 / U-8 | Postman 正向测 |

---

### 3.4 文件模块 FR-FI（File Upload / Download / Dedupe）

| FR 编号 | 功能点 | 详细要求 | 对应 BR / UC | 验证方法 |
|---|---|---|---|---|
| FR-FI01 | **单文件上传 + 秒传（去重）** | `POST /file/upload` Multipart：① 大小校验 >50MB → 5001；② 类型校验，白名单外 → 5002；③ `HashUtil.calcMd5(inputStream)` 算哈希；④ `fileStorageMapper.selectByHash(hash)`；命中 → `reference_count+1`，返回 `hitQuickUpload=true, fileStorageId=原id`（**不落盘**）；未命中 → `Files.copy` 到 `uploads/YYYY/MM/DD/hash.ext`，插入新 file_storage 记录 + 返回 `hitQuickUpload=false`。 | BR-FI01/02/03 / UC-003 | 4 次同文件 hitQuickUpload=true ✅ + 第 5 次 5003 ✅（Smoke-⑥） |
| FR-FI02 | **文件下载（见 FR-R04）** | ResourceId 路径语义（非 fileStorageId）+ 权限通过 + 计数原子 + 200 流。 | BR-FI04 | 同上 |
| FR-FI03 | **上传限流（用户维度 6/min）** | `@RateLimit(limit=6, windowSeconds=60, keyFrom="currentUserId")` AOP：以 `userId` 为 key 计数；超限抛 BusinessException(5003)。**注意：限流发生在上传接口，不限秒传（不占IO）还是都限制——MVP：两次一起限（简化，实测 Smoke-⑥ 第 5 次实际 5003 与该策略一致）**。 | BR-FI05 / AOP 骨架 | Smoke-⑥ Attempt 5 → code=5003 ✅ |
| FR-FI04 | **目录策略** | 上传相对路径：`<uploads>/<YYYY>/<MM>/<DD>/<32hex md5>.<扩展名>`，防 1 目录 10w 文件 OS 级性能下降；`fileService.resolveFile(relativePath)` 拼接绝对路径，**必须防止 `../` 目录穿越**。 | 运维 / 安全 | 代码静态：resolveFile 用 Path.normalize + startsWith(uploadsRoot) 校验 ✅ |

---

### 3.5 收藏模块 FR-F（Favorites）

| FR 编号 | 功能点 | 详细要求 | 对应 BR / UC | 验证方法 |
|---|---|---|---|---|
| FR-F01 | **收藏 / 取消收藏（幂等）** | `POST /favorite/add body={resourceId}` / `POST /favorite/remove body={resourceId}`：add 时 unique_uk `(user_id, resource_id)` 命中则跳过 insert 返回 `{existed:true, favorited:false}`，不抛 SQLIntegrityConstraintViolationException；remove 时未收藏过也不抛异常。 | BR-F01 / UC-006 | Add → Remove → Re-add 3 次 code=1 ✅ Smoke-⑤ |
| FR-F02 | **我的收藏分页列表** | `GET /favorite/list?pageNum=1&pageSize=10`：按 `favorite.create_time DESC`；仅返回「已通过审核」+「我收藏的」；左联 resource.user 拿上传者信息（resultMap 引用 `ResourceMapper.ResourceResultMap` 避免列冲突）。 | BR-F02 / U-14 | 功能类 + XML 已实现 ✅（FavoriteMapper line 108 已修复引用） |

---

### 3.6 管理后台模块 FR-AD（Admin Console）

| FR 编号 | 功能点 | 详细要求 | 对应 BR / UC | 验证方法 |
|---|---|---|---|---|
| FR-AD01 | **资源审核通过 / 拒绝** | `POST /admin/resources/review body={resourceId, approve, rejectReason?}`：DTO 自定义校验 `approve=false 时 rejectReason 必须非空`；approve=true → `review_status=1`，同时 `reviewer_id=?, review_time=NOW()`；approve=false → `review_status=2, reject_reason=?`。 | BR-R02 / AD02 / UC-004 | Smoke-③ approve=true code=1 ✅（reviewStatus=1 → 修正为 approve=true 字段） |
| FR-AD02 | **Excel 批量导入师生** | `POST /admin/users/import` Multipart `.xlsx`：解析每行 → 字段校验（学号/工号非空）→ 密码默认（MVP：默认学号后 8 位 / 或工号；不满足自动补 `1a` 后缀）→ 查重 → 合法行批量 insert；**响应体 `ImportResultResponse {total, successCount, failCount, rowErrors:[{rowIndex, field, message}]}` 按行级错误返回**；MVP 部分成功，非全事务回滚（按 BR-AD05 明细优先）。 | BR-AD04/05 / UC-007 | Service 已实现 + ImportResult.vue 已实现，完整 UI 待联调 |
| FR-AD03 | **用户管理 CRUD** | `GET /admin/users?pageNum=1&pageSize=10&keyword=&role=&status=` 分页；`POST /admin/users/create` 管理员手动新增（密码同注册规则，2004 校验）；`POST /admin/users/update/{id}` 改资料；`POST /admin/users/delete/{id}` 删账号。 | BR-AD01 / U-15 | 接口存在，CRUD 正反向用例待补 |
| FR-AD04 | **管理员重置任意用户密码** | `POST /admin/users/reset-password body={userId, newPassword}`：无需旧密码；新密码仍需 2004 强度校验；返回 code=1，提示用户"下次登录请改密"。 | BR-AD01 / U-16 | DTO / Service 已实现 ✅ |
| FR-AD05 | **资源管理（按上传者/状态/关键词过滤）** | 复用 FR-R02 动态查询，附加按 uploaderId=?、review_status=? 细筛；删除仍调用 FR-R06。 | BR-AD03 / U-18 | ResourceManage.vue 已实现 ✅ |

---

## 4. 非功能需求（Non-Functional Requirements, NFR）

### 4.1 性能 NFR-P

| # | 需求 | 量化指标 | 验证方法 |
|---|---|---|---|
| NFR-P01 | **接口响应时间** | 95% 常规 GET/POST 接口（非上传/下载）≤ **300 ms**（localhost，本机 MySQL，空库预热后） | JMeter 10 并发跑 5 轮取 P95 |
| NFR-P02 | **列表分页** | 1 万条资源数据量下，`pageSize=10` 的列表接口 ≤ **200 ms**；MySQL 需有 `idx_course / idx_uploader_id / idx_create_time` 索引（init.sql 已加）。 | 造 1 万条造数脚本测 |
| NFR-P03 | **下载吞吐** | 单个 50MB 文件下载 ≥ **20 MB/s**（本机 localhost 非生产网络，实际更高） | curl/wget 单文件测速 |
| NFR-P04 | **并发下载计数不丢失** | 用 200 并发 / 同一资源 id 下载；最终 `download_count = 前值 + 成功请求数（=200，除非鉴权不通过）** | JMeter 并发 200，查 DB 校验值 |
| NFR-P05 | **上传并发吞吐** | 每秒同时发起 10 次不同内容小文件（1MB）上传，成功率 ≥99.9%，限流阈值准确（>6/min/用户返回 5003）。 | 10 并发脚本 / 计数返回 5003 个数准确 |

### 4.2 安全 NFR-S

| # | 需求 | 详细要求 |
|---|---|---|
| NFR-S01 | **JWT 密钥与 HMAC** | `JwtConfig.secret` 长度 ≥ 256 bit；生产环境必须写环境变量 `QINGZHI_JWT_SECRET`，**禁止写死在 application.yml 里**（MVP：默认值，生产需覆盖）。 |
| NFR-S02 | **密码** | 仅 MD5 哈希入库；登录比较永不返回"账号存在 / 密码错误"的差异化错误（统一 2002），**防枚举用户名**。 |
| NFR-S03 | **SQL 注入防护** | 全链路 MyBatis `#{}` 占位符；无字符串拼接 `${}`；keyword 模糊用 `CONCAT('%', #{keyword}, '%')`。 |
| NFR-S04 | **目录穿越** | `FileServiceImpl.resolveFile()` 用 `Path.normalize()` + `path.startsWith(uploadsRoot)` 绝对保证任何 `.././` 穿越被拒。 |
| NFR-S05 | **越权访问** | 学生调管理员接口 1002；改他人资源 3002 / 1002；收藏他人无效资源返回 3001；**所有负向用例必须在 SQA 阶段 100% 通过**。 |
| NFR-S06 | **限流防刷** | 上传 6/min / 用户；登录失败 5 次锁 30min；防止同一学生瞬间 1000 次上传把磁盘写满。 |
| NFR-S07 | **统一异常** | `GlobalExceptionHandler` 捕获所有 Throwable，**永不抛 Tomcat 默认 500 HTML**给前端；永远返回统一 `{code, message, data}` JSON。 |

### 4.3 可用性 / 兼容性 NFR-A

| # | 需求 | 详细要求 |
|---|---|---|
| NFR-A01 | **浏览器兼容** | 最新版 Chrome / Edge / Firefox / Safari 两版本内。MVP 优先保证 Chrome + Edge 100%。 |
| NFR-A02 | **中文全链路 utf8mb4** | Windows/中文 username 插入、资源标题中文、拒绝理由中文、Excel 导入含中文姓名，均 **100% 正确显示 / 查询 / 检索**，不允许出现 ERROR 1366。 |
| NFR-A03 | **启动自检** | 后端启动时检查 uploads 目录存在 → 不存在自动 mkdir；启动失败（数据库连不上）立即抛错退出，不可"带病运行"。 |
| NFR-A04 | **前端路由 History 模式** | 刷新深层路由 `/resource/list` 不得 404；开发期 Vite 已配置；生产部署 Nginx 需配 `try_files $uri $uri/ /index.html`。 |

### 4.4 可维护性 / 可观测性 NFR-M

| # | 需求 | 详细要求 |
|---|---|---|
| NFR-M01 | **统一响应 / 错误码** | 所有 API 严格按 `ResponseCodeEnum` 返回；新增错误码必须加枚举并同步 PRD §5.3；**code 永不出现魔法数字**。 |
| NFR-M02 | **MyBatis SQL 日志** | 开发期 `mybatis.configuration.log-impl=StdOutImpl` 输出 SQL，便于调试；生产切 INFO。 |
| NFR-M03 | **Spring Boot Actuator** | 暴露 `/actuator/health`（白名单已放）；健康检查 DOWN 时返回 `{"status":"DOWN"}`；运维监控接入。 |
| NFR-M04 | **代码分包严格** | 严格遵循 PRD §3.4.3 分包；Maven + Vite 项目结构一致，6 个 Controller / 6 对 Service 对应 6 模块绝不混。 |
| NFR-M05 | **统一分页封装** | 所有分页统一返回 `PageResult {list, total, pageNum, pageSize}`，前端 `<Pagination>` 组件只认此契约。 |

### 4.5 可移植性 / 部署 NFR-D

| # | 需求 | 详细要求 |
|---|---|---|
| NFR-D01 | **JDK 兼容** | `release=17` 编译，可在 JDK 17 / 21 / 26 GA 运行通过（实测 JDK 26 PASS）。 |
| NFR-D02 | **跨平台** | 同样代码在 Windows 10+ / macOS 13+ / Ubuntu 22.04+ 均可启动、连接 MySQL、上传下载；**路径分隔符必须用 Path API，禁止硬编码 `\\` 或 `/`**。 |
| NFR-D03 | **文件存储可替换** | MVP 本地；未来切 S3：只需新增 `S3FileServiceImpl implements FileService`，对上层 ResourceController / FileController 无改动（开闭原则）。 |
| NFR-D04 | **配置外置** | `application.yml` 中 `datasource.password, jwt.secret` 提供 `${QINGZHI_DB_PASSWORD:默认值}` 占位符，生产用环境变量覆盖。 |

---

## 5. 接口契约（Interface Contracts）

### 5.1 全局统一响应

所有接口 HTTP Status 均为 **200**，实际语义由 body.code 决定，前端无需区分 HTTP 4xx/5xx：

```json
{
  "code": 1,
  "message": "Success!",
  "data": {}
}
```

### 5.2 标准错误码表（完整对齐 `ResponseCodeEnum`）

| 错误码 | 常量名 | HTTP 语义 | 标准前端提示 | 触发场景（示例） |
|---|---|---|---|---|
| 1 | SUCCESS | 200 OK | 不弹 / 可选绿色 Toast | 成功 |
| 0 | FAILURE | 200（内部 500 捕获） | 系统繁忙，请稍后重试 | 未预期异常 |
| 2 | PARAM_ERROR | 200 | 参数错误 | DTO 校验失败（空必填） |
| 1001 | NOT_LOGGED_IN / UNAUTHORIZED | 200 | 未登录或登录已过期 | 缺 Bearer Token / JWT 过期 |
| 1002 | NO_PERMISSION / PERMISSION_DENIED | 200 | 无权限执行该操作 | 学生走 /admin/* |
| 2001 | ACCOUNT_ALREADY_EXISTS | 200 | 账号已存在 | 重名注册 / 导入同号 |
| 2002 | ACCOUNT_OR_PASSWORD_ERROR | 200 | 账号或密码错误 | 登录不匹配（防枚举） |
| 2003 | ACCOUNT_LOCKED | 200 | 账号已锁定，请稍后再试 | 连续失败 ≥5 |
| 2004 | INVALID_PASSWORD_FORMAT | 200 | 密码格式不合法… | 注册/改密/导入初始密码太弱 |
| 2005 | USER_NOT_EXIST | 200 | 用户不存在 | 管理员查不存在 ID |
| 3001 | RESOURCE_NOT_FOUND | 200 | 资源不存在 | 无效 resourceId |
| 3002 | RESOURCE_STATUS_NOT_ALLOWED | 200 | 当前资源状态不允许该操作 | 下载他人待审核资源 |
| 4001 | EXCEL_FORMAT_ERROR | 200 | Excel 格式错误 | .xls / .csv 非 xlsx |
| 4002 | EXCEL_DATA_INVALID | 200 | Excel 数据校验失败 | 第 N 行必填空 |
| 5001 | FILE_SIZE_EXCEEDED | 200 | 文件大小超限 | >50MB |
| 5002 | FILE_TYPE_NOT_SUPPORTED | 200 | 文件格式不支持 | 白名单外扩展名 |
| 5003 | UPLOAD_RATE_LIMITED | 200 | 上传频率超限，请稍后再试 | 60s 内第 7 次上传 |
| 5004 | FILE_UPLOAD_FAILED | 200 | 文件上传失败 | 落盘 IO 异常 / Hash 计算异常 |
| 5005 | FILE_NOT_FOUND | 200 | 文件不存在 | 引用计数删光了文件物理被删 |

### 5.3 核心 API 速查（与前端 `api/*.js` 对齐）

| 模块 | 方法+路径 | 鉴权 | 请求体（主要字段） | 响应 data |
|---|---|---|---|---|
| 认证 | POST /auth/register | 匿名 | username, password, role(1/2), name, department, major, phone, email | `{id, username}` |
| 认证 | POST /auth/login | 匿名 | username, password | `LoginResponse {token, userInfo}` |
| 用户 | GET /user/info | 登录 | Query 无 | `UserInfoResponse` |
| 用户 | PATCH /user/info | 登录 | name, phone, email, department, major | `{updated:true}` |
| 用户 | POST /user/avatar | 登录 | Multipart avatar | `{avatarUrl}` |
| 用户 | POST /user/change-password | 登录 | oldPassword, newPassword | `{changed:true}` |
| 资源 | GET /resource/list | 公开/登录 | keyword, course, startTime, endTime, pageNum, pageSize | `PageResult<ResourceListResponse>` |
| 资源 | GET /resource/{id} | 公开/登录 | （PathVariable 数字） | `ResourceDetailResponse` |
| 资源 | POST /resource/publish | 登录 | title, description, course, +6文件字段 or fileStorageId | `{resourceId}` |
| 资源 | POST /resource/update | 登录（本人/管理） | id, title, description, course, +6文件字段 | `{updated:true}` |
| 资源 | POST /resource/delete/{id} | 登录（本人/管理） | （PathVariable） | `{deleted:true}` |
| 收藏 | POST /favorite/add | 登录 | resourceId | `{favorited, existed}` |
| 收藏 | POST /favorite/remove | 登录 | resourceId | `{removed:true}` |
| 收藏 | GET /favorite/list | 登录 | pageNum, pageSize, keyword | `PageResult<FavoriteListResponse>` |
| 文件 | POST /file/upload | 登录（限流6/min） | Multipart file | `{fileStorageId, fileName, fileSize, fileHash, hitQuickUpload, filePath}` |
| 文件 | GET /file/download/{resourceId} | 登录 + 权限通过 | （PathVariable） | 文件流 200 |
| 管理员 | POST /admin/resources/review | 管理员 | resourceId, approve(Boolean), rejectReason? | `{reviewed:true}` |
| 管理员 | GET /admin/users | 管理员 | keyword, role, status, pageNum, pageSize | `PageResult<UserInfoResponse>` |
| 管理员 | POST /admin/users/create | 管理员 | 同注册字段（含 password） | `{id, username}` |
| 管理员 | POST /admin/users/update/{id} | 管理员 | 资料字段 | `{updated:true}` |
| 管理员 | POST /admin/users/reset-password | 管理员 | userId, newPassword | `{reset:true}` |
| 管理员 | POST /admin/users/delete/{id} | 管理员 | （PathVariable） | `{deleted:true}` |
| 管理员 | POST /admin/users/import | 管理员 | Multipart .xlsx | `ImportResultResponse` + `rowErrors[]` |
| 管理员 | GET /admin/resources | 管理员 | keyword, uploaderId, status, pageNum, pageSize | `PageResult<ResourceDetailResponse>` |
| 健康 | GET /actuator/health | 匿名 | — | `{status:"UP"}` |

---

## 6. 数据模型（Database Data Model）

> 详细字段 / 约束见 PRD §4 + init.sql 实际 DDL，这里仅以 4 张表 ER + 索引 + 约束摘要。

```
┌──────────────┐           ┌──────────────────┐          ┌──────────────────┐
│     user      │ 1      N │    resource       │ 1      N │    favorite      │
├──────────────┤           ├──────────────────┤          ├──────────────────┤
│ id BIGINT PK  │◄─────────│ uploader_id FK    │◄─────────│ user_id FK (UK1)│
│ username UNIQ │           │ id BIGINT PK      │─────────►│ resource_id(UK2)│
│ password(MD5)│           │ title/course/desc │          │ create_time      │
│ role(0/1/2)  │           │ 6 文件字段         │          └──────────────────┘
│ status/lock  │           │ review_status (idx)│
│ login_fail…  │           │ download_count    │
│ create_time  │           │ reject_reason     │
└──────────────┘           │ reviewer_id/time  │         ┌──────────────────────┐
                           └──────────────────┘         │      file_storage     │
                                                         ├──────────────────────┤
                                                         │ id PK                 │
                                                         │ file_hash (UNIQUE)    │
                                                         │ file_path/size        │
                                                         │ original_file_name    │
                                                         │ file_ext              │
                                                         │ reference_count       │
                                                         │ create_time           │
                                                         └──────────────────────┘
```

### 关键约束摘要（对照已实现 init.sql 实测）

| 约束名 | 类型 | 位置 | 保障的 FR/NFR |
|---|---|---|---|
| `uk_username` | UNIQUE | user.username | FR-A01 防重复注册 |
| `idx_role / idx_status` | INDEX | user.role / user.status | FR-AD03 列表高性能分页 |
| `idx_uploader_id / idx_course / idx_create_time` | INDEX | resource 三列 | NFR-P02 1w 条 200ms 达标 |
| `unique_uk_user_resource` | UNIQUE | favorite.(user_id, resource_id) | FR-F01 幂等，防重复收藏 |
| `uk_file_hash` | UNIQUE | file_storage.file_hash | FR-FI01 秒传，同哈希永不重复落盘 |

---

## 7. 部署与运维需求（Deployment & Ops）

| # | 需求 | 详细说明 |
|---|---|---|
| OPS-01 | **一键构建产物** | 后端：`mvn -DskipTests package` → `backend/target/qingzhi.jar`；前端：`npm run build` → `frontend/dist/`（已实测 26 个 vue chunk 生成 ✅）。 |
| OPS-02 | **启动脚本（Windows / Linux 二选一推荐）** | 开发期已覆盖 `mvn spring-boot:run`；生产：`java -Dfile.encoding=UTF-8 -DQINGZHI_DB_PASSWORD=xxx -jar qingzhi.jar`。 |
| OPS-03 | **数据库备份策略** | MySQL 每日凌晨全量 `mysqldump qingzhi_platform --default-character-set=utf8mb4`；保留 30 天。 |
| OPS-04 | **文件备份** | `backend/uploads/` 目录每日同步至备份盘 / 对象存储；定期校验 `reference_count=0` 的孤儿文件做归档清理。 |
| OPS-05 | **监控 / 告警** | 接入 `/actuator/health`；CPU >85% / 磁盘剩余 <10GB / 连续 5 分钟 5xx>1% → 告警。 |
| OPS-06 | **init.sql 正确执行方式** | 严格按 init.sql 顶部注释：`mysql -uroot --default-character-set=utf8mb4 < init.sql`；Windows PowerShell 执行失败时走 `cmd /c`。 |

---

## 8. 需求验证矩阵 & 交付验收（Traceability & Acceptance）

### 8.1 BR / UR / FR / 验证 四层可追溯矩阵

| 业务需求 BR | → | 用户用例 UR | → | 系统功能需求 FR | → | 实际验证（当前实现状态 2026-08-26） |
|---|---|---|---|---|---|---|
| BR-A01 双通道注册 | → | UC-001 注册 | → | FR-A01/A02 | ✅ Smoke-② 学生注册 code=1 |
| BR-A03 预置 Admin | → | UC-002 登录 | → | FR-A03/A05/A06 | ✅ Smoke-① + UI 真登录 Dashboard 跳转 |
| BR-A05 登录锁定 | → | UC-002 异常流2 | → | FR-A05 (login_fail_count) | ⭕ 骨架（字段+2003 枚举已就位，Service 阈值细化待补） |
| BR-A06 JWT + 匿名公开 GET | → | 匿名游客访问列表 | → | FR-A06/A07 | ✅ 匿名 GET code=1 ✅（脚本 Case 1/2） |
| BR-P01/P03 RBAC 后端兜底 | → | 学生调审核接口 1002 | → | FR-P01/P02 | ✅ JwtInterceptor + PermissionUtil |
| BR-R01/R02 发布+审核流转 | → | UC-003 + UC-004 | → | FR-R01 + FR-AD01 | ✅ Smoke-② publish → Smoke-③ approve=true |
| BR-R03 分页检索 | → | UC-005 搜索 | → | FR-R02 | ✅ SQL `<if>` 动态 + 公开 GET 匿名 |
| BR-R04 原子计数下载 | → | UC-005 下载 | → | FR-R04 + FR-FI02 | ✅ HTTP 200 + SQL rows>0 |
| BR-R05 本人改删 | → | U-7/8 | → | FR-R05/R06 + FR-P02 | ✅ Service 层本人/管理员校验器 |
| BR-F01 收藏幂等 | → | UC-006 | → | FR-F01 | ✅ Smoke-⑤ 3 次 code=1 |
| BR-FI01/02/03/05 文件能力 | → | UC-003 上传 | → | FR-FI01 + FI03 + FI04 防穿越 | ✅ Smoke-⑥ 4 次秒传 + 第5次 5003 |
| BR-AD01/04 用户管理 + 导入 | → | UC-007 林老师导入 | → | FR-AD02 + FR-AD03/04 | ✅ AdminServiceImpl（ExcelUtil + ImportRowError） |
| BC-02 utf8mb4 三层 | → | UX-06 中文绝对正常 | → | NFR-S 安全 + NFR-A 可用 | ✅ HEX 字节精确匹配 超级管理员/系统管理 ✅ |
| UX-05 可见即所得过滤 | → | Sidebar 隐藏管理菜单 | → | FR-P03 | ✅ Dashboard snapshot 学生/教师不显示管理入口 ✅ |

### 8.2 交付验收总闸

交付前必须 100% 同时满足：

1. **7/7 核心场景冒烟 PASS**：执行 `smoke.ps1` FINAL SUMMARY 全部绿色 ✅（当前已满足）
2. **负向用例套件 100% PASS**：1001/1002/2001/2003/3001/3002/5001/5003 共 8 个错误码至少各 1 条 Postman 用例。
3. **mvn compile + npm run build 双零报错**：后端无编译警告；前端无 ESLint / Vite Build 错误。
4. **UTF-8 全链路零乱码**：至少 10 条中文资源 / 姓名 / 院系 CRUD + 模糊检索正确。
5. **UI 层登录 → 资源列表 → 详情 → 下载 → 收藏**完整人工手测，无控制台 Error。
6. **README / 三份需求文档**齐备，目录链接可点击跳转。

> 📌 **当前实现状态（2026-08-26）**：已完成上述验收项 ①/③/④（字节级）/⑤（UI Dashboard）/⑥；剩余②（负向 Postman 套件）和 ⑤ 的资源列表真数据联调属于后续 SQA 迭代项。
